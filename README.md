# LaserAlignment
## Capturing a single image
1. SSH into Rpi: <br>
`ssh pi@<IP address>` $\rightarrow$ password: `rasberry`
2. Install this repository <br>
`git clone https://github.com/Jsolsky/LaserAlignment.git`
3. Navigate to the repositry
4. Run script in terminal using the command: <br>
`python camera_test.py`
5. Open image saved as `test.jpg`